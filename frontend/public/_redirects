# Redirects/Rewrites para Render Static Site
# Atualizado em 04/12/2025 - Ordem importa: regras mais específicas primeiro

# IMPORTANTE: Proxy de API para o backend
# Todas as chamadas /api/* devem ir para o backend (api.agendaaquivistorias.com.br)
/api/*      https://api.agendaaquivistorias.com.br/api/:splat  200

# Páginas específicas (REWRITE - mantém URL)
/admin      /admin.html             200
/admin/*    /admin.html             200
/painel     /painel-empresa.html    200
/painel/*   /painel-empresa.html    200

# Arquivos estáticos (passthrough)
/assets/*   /assets/:splat  200
/*.js       /:splat.js      200
/*.css      /:splat.css     200
/*.svg      /:splat.svg     200
/*.png      /:splat.png     200
/*.jpg      /:splat.jpg     200
/*.ico      /:splat.ico     200
/*.woff     /:splat.woff    200
/*.woff2    /:splat.woff2   200

# Slugs de empresas - paths dinâmicos
/:slug      /empresa.html   200

# Fallback SPA
/*          /index.html     200
